name: Daily Commit Automation

on:
  schedule:
    - cron: "0 7 * * *" # Runs every day at 07:00 UTC (12:30 IST) - Recommended for same-day attribution
    - cron: "0 19 * * *" # Redundant run to ensure coverage
  workflow_dispatch: # Allows manual trigger for testing

jobs:
  commit:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
        with:
          persist-credentials: true
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Run Daily Commit Script
        run: |
          git config --global user.name "AgamArora20"
          git config --global user.email "agamarora91@gmail.com"
          node dailyCommit.js
          
      - name: Success Check
        run: |
          echo "Commit and Push completed."
          node checkToday.js
